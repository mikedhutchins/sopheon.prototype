@model Sopheon.Web.App.Models.PanelViewModel
@using Sopheon.framework;
@using System.Linq;
<div id="@Model.Id" class="sopheon-panel left @string.Join(" ", Model.CssClasses.ToArray())" @Model.AttributeMl >
    <div class="primary-panel-nav @(Model.HideNav ? "hide" : string.Empty)">
        <ul class="primarynavigation">
            <li class="small-tile button-tile permanent sangre-scroll" data-scroll-container="#sopheon-main" data-scroll-target="#mainNav" data-scroll-duration="400">Home</li>
        </ul>
        <div class="clear"></div>
    </div>
	@RenderBody()
</div>
